{
    "scripts": {
        "build": "npm run build:wasm && npm i && tsc --project \"tsconfig.json\"",
        "build:wasm": "wasm-pack build --target nodejs --out-dir ../../typescript-id-allocator/dist",
        "clean": "rimraf ./dist"
    },
    "devDependencies": {
        "rimraf": "^3.0.2",
        "typescript": "^4.8.3",
        "wasm-pack": "^0.10.3"
    },
    "dependencies": {}
}